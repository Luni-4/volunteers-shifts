## Default profile.
[default]
address = "127.0.0.1" ## localhost address
template_dir = "templates"
port = 8080

## Smartphone profile.
## Anyone should replace both IP and port with their own values in order to
## reach the device where the app is running on.
[mobile]
address = "0.0.0.0"
port = 8080
